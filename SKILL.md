---
name: task-clarifier
description: >
  当用户面对一个任务但说不清楚目标、不确定该给 AI 提供什么背景/上下文、
  不确定现有 skills/MCP/插件能力够不够、或者只能下达模糊指令时，使用此 skill
  先做一次"任务侦查"，再产出精确指令。触发场景包括但不限于：用户说"我不知道
  怎么描述这个需求"、"这个任务该给你看哪些文件"、"帮我理一下思路再让你做"、
  或者用户下达的任务明显笼统（如"优化一下这个模块""帮我重构一下""看看这个
  怎么弄"）却没有给出验收标准和相关材料时，主动建议或直接使用本 skill 先诊断
  再执行，而不要直接草率上手干活。编程任务（重构、新功能、调试、迁移）优先，
  但同样适用于写作、分析、调研等通用任务。
---

# Task Clarifier / 任务侦查与指令构建器

## 核心理念

用户的"模糊感"通常不是不会表达，而是有四层信息缺口没有被显式化：
**目标边界不清 → 该给的上下文没盘点 → 工具/能力够不够不知道 → 于是写出来的指令是一段大白话。**

这个 skill 的工作方式是 **"先自查、后追问、多轮收敛"**：能自己查到的信息（读文件、
扫目录、查已有 skills/MCP、看 git 历史）先查清楚，不拿这些去烦用户；查不到、且会
影响执行结果的信息，通过**分批提问**向用户追问，一轮问完根据回答判断是否还有新的
缺口，需要就继续追问下一轮，直到信息足够生成精确指令为止。

**追问不是无限连问**：每轮只问当前最关键、最影响后续判断的问题（通常 2-4 个），
问完这轮再决定要不要有下一轮，避免用户被一次性甩一堆问题淹没，也避免陷入来回拉扯。

## 工作流

### Step 0：自动侦查（不要问用户，先自己看）

在编程场景下，主动做：
- `view` 相关目录结构，定位任务可能涉及的文件
- 读项目根目录的 `CLAUDE.md` / `README` / `.cursorrules` 等约定文件
- 检查 `skills/` 目录下已有哪些 skill，判断任务是否已被某个 skill 覆盖
- 检查已连接的 MCP 列表，判断任务需要的能力（如访问某系统、读某类文件）是否已具备
- 如果是修改已有代码，扫一眼相关文件的最近改动（git log / blame）来推断设计意图

在通用任务场景下，主动做：
- 复用当前对话里已经出现过的所有相关信息（用户之前提到的偏好、约束、目标）
- 如果用户上传了文件，先读完再假设

**原则：能自己查到的，不问用户。只对"查不到、且影响结果"的信息做假设标记。**

### Step 1：整理侦查结果 + 列出第一轮问题

先把 Step 0 查到的事实简要列出来（让用户知道你已经做了功课，不是从零问起），
再针对**查不到、且影响后续判断**的缺口，给出第一轮问题。格式：

```markdown
## 已经确认的信息（来自侦查，不用你回答）：
- [文件/约定/历史决策 1]
- [文件/约定/历史决策 2]

## 第一轮问题（2-4个，只问当前最关键的）：
1. [目标/边界类问题]
2. [验收标准类问题]
3. [如有必要：能力缺口相关问题]
```

**问题排序原则**：先问会决定"整个任务方向"的问题（比如优化目标是哪个维度），
再问会影响"具体做法"的问题（比如某个字段的处理优先级）——避免先问细节，
回头因为方向性问题推翻重来。

**每个问题都要是选择题或者可以一句话回答的问题**，不要抛开放式大问题让用户重新组织语言，
这违背了"用户本来就不知道怎么描述"的前提。

### Step 2：根据回答判断是否需要下一轮

拿到用户第一轮回答后，自问：**"现在的信息够不够我写出一份不会被推翻的执行指令？"**

- 够了 → 直接进入 Step 3，不要为了"显得严谨"硬凑下一轮问题
- 不够（比如回答暴露了新的分支/新的不确定性）→ 提出第二轮问题，同样控制在
  2-4 个，且必须是被第一轮回答**引出**的新问题，不能是本可以在第一轮就问的问题
  （如果发现是这种情况，说明第一轮问题设计得不够周全，下次要吸取教训）

一般到第二轮就应该收敛；如果第三轮还在问，大概率是任务本身还没想清楚要不要拆分
成多个子任务，这时候应该跳出追问循环，直接跟用户说"这个任务可能需要先拆成几个
子任务分别处理"，而不是继续无限追问下去。

### Step 3：生成最终可执行指令

根据用户的确认/修正，产出一段**可以直接粘给 Claude Code / Codex 的结构化指令**，
遵循以下结构（不需要每次都用 XML 标签，但结构要显式）：

1. **背景**：一两句话说明这段代码/任务在整体系统里的位置
2. **具体任务**：明确到"改哪个函数/生成哪种文件/输出什么格式"的颗粒度
3. **必须遵守的约束**：风格、性能、兼容性、"不要做"的红线
4. **参考材料**：明确列出该读哪些文件/示例（路径要具体，不要"相关文件"这种虚指）
5. **验收标准**：怎么判断这次输出是合格的
6. **复杂任务**：显式要求"先列出执行计划，再动手"，避免 AI 一步到位跑偏

## 编程场景专项检查清单

编程任务优先，产出简报前额外检查：
- [ ] 涉及的文件路径是否列全（包括被间接依赖但容易漏掉的配置/类型定义文件）
- [ ] 是否存在项目既有代码风格/命名约定（从已有代码里提炼，而非让 AI 自己猜）
- [ ] 是否有现成的测试用例可以作为"正确性"的锚点；没有的话是否需要先让 AI 写测试
- [ ] 这个改动是否牵涉多个模块的协同（如果是，指令里要显式列出改动顺序/依赖关系）
- [ ] 是否需要先跑一次现状确认（如现有报错信息、当前行为截图）再动手，而不是凭描述猜

## 通用任务（写作/分析/调研）简版检查清单

- [ ] 受众是谁、用途是什么（内部讨论稿 vs 对外发布，标准完全不同）
- [ ] 篇幅/格式要求（字数、是否要小标题、是否要列表）
- [ ] 是否有"反例"——用户明确不想要的风格/结论，比这有什么风格更重要
- [ ] 是否需要引用/核实外部信息（决定要不要先做检索再动笔）

## 输出后的自检

给出最终指令前，用一句话问自己：**"如果我是执行这条指令的 AI，我还需要反问什么？"**
如果答案不是"没有了"，说明简报还有遗漏，回到 Step 0 补侦查，而不是把问题甩给用户。

## 何时判断"能力缺口"需要停下来而不是硬做

如果侦查发现任务需要的能力（特定领域知识、需要访问的系统、需要的文件格式处理能力）
明显超出当前 skills/MCP 覆盖范围，**不要让 AI 硬凑一个退化方案蒙混过关**——把这个
缺口作为追问轮次里的一个问题明确提出，给用户两个选项：(a) 先补齐能力（连接 MCP /
新写一个 skill）再执行，或 (b) 明确降级到"能力范围内的替代方案"并说明会损失什么。
让用户自己选，不要替用户悄悄降级，也不要把这条缺口埋在一堆细节问题里不显眼。
